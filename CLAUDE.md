# BidWatch — 공공 입찰공고 키워드 모니터링 SaaS

FastAPI + PostgreSQL 백엔드, Next.js 프론트엔드로 만드는 구독형 입찰공고 수집·매칭 서비스.
(2026-09-23 greenfield 템플릿 체계를 기존 저장소에 이식 — Phase 001~008은 이식 전에 완료됐다.)

## 현재 단계
- 전체 Phase 체크리스트/아키텍처의 **단일 원본 = `work_log/plan.md`** (여기에 중복 기재 금지)
- 현재: **Phase 009 — 자동 수집** (Phase 완료 시 이 줄과 plan.md 체크박스를 함께 갱신)

## 새 세션 시작 시
- 훅(`session_context.py`)이 plan.md의 현재 Phase·읽을 Phase 로그·실테스트 대기를 주입한다 — 지시대로 읽는다.
  **세션 첫머리에 `[session_context]` 줄이 안 보이면 훅이 죽은 것이다** — `work_log/plan.md`부터 직접 읽고 훅을 고친다.

## 불변 규칙
- **Silent failure 절대 금지**: 모든 예외 상황을 명시적으로 로깅한다. 빈 except/catch 금지.
  넘어가야 할 정당한 이유가 있으면 그 이유를 로그+주석으로 남긴다.
- **조용한 절단 금지**: 결과를 상한으로 자르면 **잘랐다는 사실과 전체 건수를 함께** 돌려준다.
  페이지네이션 첫 페이지만 받기·라이브러리 기본 limit·로그 꼬리처럼 **암묵적으로 잘리는 것도 절단이다.**
  목록·검색 기능의 수용 기준에 "N건을 넘으면 전체 건수와 '더 있음'을 표시"를 넣어 테스트로 건다.
- **손으로 만든 데이터에는 자동 검증을 붙인다**: 사람이 쓴 매핑·목록은 반드시 틀린다
  (예: 지역 별칭 표 → `test_all_aliases_normalize_into_known_regions`).
- **허용의 기준은 "위험"이다 — 파괴적이지 않으면 연다** (2026-08-22 사용자 결정, 종전 "허용 목록을
  넓히지 않는다" 대체. 1인·서버 없음 전제 — 협업·운영 서버가 생기면 다시 묻는다).
  **루틴 작업(커밋·테스트·빌드·설치·조사)에 승인 창이 뜨면 그것이 결함이다.** 파괴적 명령은 deny,
  받은 실행물 실행·패키지 **추가**(잠금 파일에 없는 새 의존성)는 건별 확인 — 잠금 파일대로 설치하는
  `npm ci`·`pip install -e`(pyproject 기준)는 추가가 아니라 설치이므로 연다. 반복되면 ① 허용된 형태로 부른다(습관 — 반복되면 훅)
  ② 규칙 추가. 점검은 Phase 종료 시 `/approval-audit`. 대가·목록 실물: `노하우_승인_대기_최소화.md`
- **비밀정보(API 키, 접속정보)는 이 파일·커밋·채팅에 절대 쓰지 않는다.** `backend/.env`에 두고 gitignore한다.
- **로컬 포트는 9000번대**: 프론트 9000 / 백엔드 9100 / 프로토타입 9456. 새 서비스도 9000번대에서 고른다.
- **DB 시각은 `func.now()`로 DB가 찍게 한다** (2026-09-23 실측 정정): 실제 컬럼은 전부 `timestamptz`, 세션 TimeZone은
  Asia/Seoul인데 모델은 timezone 없는 `DateTime`이다 → aware datetime은 저장 실패, naive `utcnow()`는 KST로 해석돼
  **9시간 이르게** 저장된다(2026-09-23 기존 3곳도 `func.now()`로 교체, 기존 행 보정은 안 함 — 사용자 결정). `SCHEMA.md`.

## 데이터 모델/스키마 변경 게이트 ★스키마는 되돌리기 비싸다
- 스키마 **결정의 기록 = `docs/SCHEMA.md`** (설계 의도, 관계, 제약, 변경 이력).
- **AI는 임의로 컬럼 추가/변경, 마이그레이션 수정을 하지 않는다.**
  ① 변경안(무엇을·왜·영향 범위)을 SCHEMA.md에 먼저 기록 → ② 사용자 확인 → ③ 반영.
- **SCHEMA.md는 "결정과 이유"를, 코드(`backend/app/models/`, `backend/alembic/versions/`)는 "현재 상태"를 담당한다.**
  둘이 어긋나면 코드가 정답이고 SCHEMA.md를 고친다. 컬럼 목록을 손으로 이중 관리하지 말 것.
- **사용자 데이터와 빌드 산출물을 같은 파일·DB에 두지 않는다** — 산출물은 재빌드 때 통째로 교체되어
  그 안의 사용자 데이터가 지워진다. 저장·캐시는 별도 파일로.
- **누적되는 저장소의 스키마 변경에는 Alembic 마이그레이션을 같은 커밋에** — `create_all`은 기존 테이블을
  고치지 않는다. 상세: `노하우_설계.md`
- **수집 시점 정규화(지역 등) 로직을 바꾸면 기존 행에는 반영되지 않는다** — 백필 스크립트
  (`backend/scripts/backfill_regions.py`, `--dry-run` 먼저)를 같은 작업에서 돌린다.

## 작업 트랙 ★저위험이 기본값이다 — 기준은 "몇 줄이냐"가 아니라 "되돌리기가 비싼가"

전제: 1인 프로젝트(협업 시 재정의). 병목은 왕복 횟수다. 역사: `docs/playbooks/노하우_작업_트랙.md`

- **일반 트랙은 ① 데이터 ② 기본 구조에 닿을 때만** — 그 외 전부(화면·문구·설정·배치, 새 기능 포함)는
  저위험. ① 스키마, 빌드 파이프라인·보정 데이터, 기존 저장을 못 읽게 되는 저장 형식 변경
  (저장 파일을 하나 **더 늘리는 것**은 제외) ② 다중 구현(parity) 묶음, 동결 경계, 디렉토리 뼈대,
  화면·모듈 전환 구조. 애매하면 저위험으로 시작하고, 닿으면 멈추고 **사용자에게 묻는다** —
  혼자 일반 트랙으로 올리지 않는다. 검증은 사다리의 **일반 트랙 갈래**(구버전 데이터 실측·되돌리기 경로).
  bidwatch의 ② 실물: Alembic 마이그레이션 · bid-collectors 인터페이스(`docs/interface.md`, 양쪽 저장소 동기) ·
  테넌트 데이터 분리(공유 공고 vs 테넌트별 데이터) · 인증/역할 판정(`backend/app/deps.py`) · 수집 시점 정규화.
- **저위험 실행**: 한 턴에 구현→검증(사다리대로)→**커밋**. **배포는 사용자 실테스트가 필요한 변경일 때만**
  하고 그때 실테스트 하나만 묻는다 — 주석·내부 로직 수정은 커밋에서 멈춘다. 보고 10줄 이내, 커밋은
  묻지 않는다. 작업 중 발견한 무관 결함이 5분짜리면 그 자리에서 고친다(커밋만 나눈다).

## 작업 속도 규칙 ★실제로 샌 시간을 재고 정한다
- **느리다고 느껴지면 테스트가 아니라 승인 대기부터 잰다**(실측: 테스트 30초 vs 대기 6분).
  도구: `scripts/measure_wait.py`(벽시계 실측)·`measure_approvals.py`(원인 분류, 예측).
  처방: `docs/playbooks/노하우_승인_대기_최소화.md`
- **★ 검증 강도는 트랙이 아니라 "무엇을 바꿨나"로 정한다** (2026-09-05 사용자 결정):
  문서·주석·문구(동작 불변) = diff 읽기, 코드 파일이면 정적 분석 1회, 테스트 0 /
  **★ 표시 변경(사용자에게 보이는 모양·배치·마우스 반응·색, 2026-09-18 사용자 결정)** = (**모양을 AI가 골라야 할 때만** 목업 → 사용자 "이대로" →
  — 사용자가 모양을 명확히 지시했으면 목업 없이 바로) 구현 → 정적 분석 1 → **커밋·push → 사용자가 본다. 그것이 수용 기준이다. 자동 테스트를 새로 만들지 않는다**(그러니
  변이 확인도 없다). 전체 테스트는 **push 뒤 백그라운드 1회**(기존 테스트가 깨졌는지만 — 깨지면 후속 커밋). 단 **눈으로
  안 보이는 동작**(복사 결과·건수·저장·로그·다른 화면 영향)은 자동 테스트다. 근거: `노하우_검증.md` '표시 변경은 사용자가 본다' /
  설정·배치·눈으로 안 보이는 화면 동작(동작 변경, 로직 아님) = 정적 분석 1 + 관련 테스트 1(관련 = 바뀐 파일을 직접 쓰는 테스트) /
  로직 변경 = 정적 분석 1 + 관련 테스트 1 / **변이 확인은 테스트를 새로 넣거나 고쳤을 때만**('검증 원칙') —
  변이는 코드가 아니라 테스트를 검증하는 것이라 테스트를 안 건드린 한 줄 수정에는 붙지 않는다. **기존 단언의
  기대값만 바꾼 것(20→10)도 붙지 않는다**(잡는지는 만들 때 봤다). **변이를 되돌린 확인은 재실행이 아니라 diff로**
  — 변이 전의 통과 실행이 그대로 유효하다 /
  커밋 직전 전체 실행 = 로직이 바뀐 커밋만. 생략에는 근거(의존 그래프). 외부 API 실측·문서 갱신은 줄이지 않는다.
  **일반 트랙(데이터·기본 구조)은 종류가 다른 검증을 탄다** (2026-09-06): 전체 테스트 + **구버전 저장본을
  실제로 읽어 마이그레이션 실측**(새 코드로 만든 빈 DB는 통과하고 사용자가 쌓은 옛 DB만 깨진 실사례 —
  `노하우_설계.md`) + 되돌리기 경로(백업·롤백) 확인 + 다중 구현이면 대조 테스트 존재 확인 +
  **배포 전 사용자 실테스트 필수.**
- **★ 긴 명령이 곧 긴 승인 대기다**(38초 vs 83초 실측). 웹 페이지는 `WebFetch`가 기본, 숫자·원문 확인은 curl(허용됨 — WebFetch 요약이 숫자를 틀린 실사례) /
  `python -c` 인라인·세션 UUID가 든 스크래치패드 절대경로 금지(훅) / 한글 인자를 셸 명령에 쓰지 않는다 /
  일회성 스크립트는 gitignore된 `scripts/_tmp/`에 덮어써서 쓰고 **지우지 않는다**(삭제는 매번 승인) /
  **일회성 명령은 "항상 허용"으로 열지 않는다** — 다시 안 걸리는 죽은 규칙만 남는다.
- **커밋은 작업 단위마다.** 여러 줄 메시지는 `.commit_msg.txt`에 쓰고 `git commit -F .commit_msg.txt`.
  검증 결과(테스트 범위·수용 기준 대조·**대체 수단을 썼으면 그 사실과 이유**)는
  Phase 로그가 아니라 **커밋 메시지에** 적는다 — 조용히 건너뛰지 않는다.

## 요구사항 정책 ★"정답 기준"이 없으면 완료를 판단할 수 없다
- 기능 요구사항 + 수용 기준의 **단일 원본 = `docs/REQUIREMENTS.md`**
- **구현 전**: 수용 기준이 먼저 있어야 한다 — 없으면 스펙화하고 모호점은 사용자에게 질문.
- **요구사항이 바뀌면 즉시 갱신** — 대화 속 합의로만 남기면 다음 세션이 구버전으로 구현한다.
- **수용 기준은 검증 가능한 문장으로**("X를 넣으면 Y가 N건") — 그대로 테스트 케이스가 된다.
- **YAGNI**: 문서에 없는 요구사항을 위한 추상화·설정화·일반화 금지.
- 한 번에 훑기 어려워지면(대략 기능 15~20개) 도메인별로 분할하고 REQUIREMENTS.md는 목차+링크 인덱스로.
- **상태 어휘를 구분한다: 미착수/진행/완료/동결/중단/폐기/안 함.** 보류와 "안 할 것"을 섞지
  않고, 닫은 결정에는 날짜·근거를 남긴다(조사 기록은 지우지 않는다). 상세: `노하우_프로세스.md`
- **요구사항 ID 대장 — 새 번호는 여기서 받아간다.** 아이디어 단계여도 번호를 잡았으면 적는다.
  **번호를 재사용하지 말 것.** 발급 시 plan.md 보류 항목의 후보 번호까지 훑는다(`노하우_프로세스.md`).
  새 번호는 **새 기능에만** — 앞 기능의 마무리·문구는 앞 절 한 줄 수정, 작은 기능은 한 줄 형태.

  | ID | 기능 | 상태 |
  |---|---|---|
  | F-001 | 계정·인증 (회원가입·로그인·토큰 갱신·비밀번호 변경) | 완료 |
  | F-002 | 역할 기반 접근 제어 (owner/admin/member) | 완료 |
  | F-003 | 공공 API 수동 수집 (연쇄 수집 포함) | 완료 |
  | F-004 | 공공 API 출처 구독 | 완료 |
  | F-005 | 키워드 관리 | 완료 |
  | F-006 | 공고 목록 (키워드 자동매칭·검색·필터·페이지네이션) | 완료 |
  | F-007 | 공고 상세 (2단계 로딩·상세 보충) | 완료 |
  | F-008 | 입찰 예고 (나라장터 사전규격) | 완료 |
  | F-009 | AI 스크래퍼 — URL 추가·분석·미리보기·구독 | 진행 |
  | F-010 | 태그 + 검토요청 페이지 | 완료 |
  | F-011 | 지역 필터 + 관심 지역 | 완료 |
  | F-012 | 대시보드 | 미착수 |
  | F-013 | 자동 수집 (정기 스케줄 + 수집 이력) | 미착수 |
  | F-014 | 중복 공고 묶음 표시 (여러 출처의 같은 공고를 1건으로) | 미착수 |
  | F-015 | 고객사 관리자 — 사용자설정 마무리 (테넌트 owner/admin 기능) | 미착수 |
  | F-016 | 운영자 콘솔 (플랫폼 운영자 역할 분리 + 전 테넌트 관리 화면) | 미착수 |
  | F-017 | 나라장터 공고 차수 정리 (취소 반영 + 최신 차수만 목록) | 진행 |
  | F-018 | 사전규격 ↔ 본 공고 연결 | 진행 |
  | **다음 번호** | **F-019** | |

## 검증 원칙
- **"됐다"는 실제 결과로 판단한다.** 빌드 성공·프로세스 살아있음은 근거가 아니다.
  실제 응답을 확인하거나 데이터를 직접 질의한다. **화면은 개발자가 본다** — AI는 눈으로 안 보이는 동작의 자동 테스트까지
  하고 plan.md '사용자 실테스트 대기'에 올린다('검증 강도'의 표시 변경 갈래). 브라우저 패널·스크린샷 자동화로
  대신하지 않는다(2026-09-15 템플릿 결정, 2026-09-23 bidwatch 채택).
- **기능 완료 시**: REQUIREMENTS.md의 수용 기준을 하나씩 짚어 대조하고 → **그 자리에서 REQUIREMENTS의
  상태와 plan.md를 둘 다 갱신한다.** 코드가 맞아도 정답지가 틀리면 다음 세션이 오독한다.
- **Phase 완료 시**: 전체 테스트 통과가 다음 Phase의 선행 조건. plan.md 체크·커밋·push까지 끝나면
  **"새 Phase는 새 세션을 열어 진행하기를 권고합니다"라고 말하고 멈춘다** — 다음 Phase를 이 세션에서
  시작하지 않는다. 이어받기는 plan.md와 Phase 로그가 한다.
- **새로 넣거나 고친 테스트는 실제로 무언가를 잡는지 확인한다** — 대상 코드를 일부러 틀리게 만들어
  실패를 한 번 본다(변이가 실제로 적용됐는지까지 확인, 한 번에 하나씩). "넘치지 않는다·깨지지 않는다"류는
  실제 값(폭·크기·건수)을 재야 잡는다 — 존재 확인(find류)은 잘려도 통과한다.
- **자동 테스트/코드 리뷰/사람 실테스트는 잡는 결함의 층이 다르다 — 서로 대체 불가.**
  실테스트에는 "고장 상태를 만들어야 드러나는" 시나리오를 명시적으로 요청한다.
- **표본 2건은 실측이 아니다** — 기록에 표본 수·확인 파라미터를 함께 적는다.
- 자동화 불가 항목(실기기·실결제)은 plan.md "사용자 실테스트 대기"에 — **확인 전 완료 선언 금지.**
- 상세(변이 확인법, 검증의 층, 디버깅 원칙): `docs/playbooks/노하우_검증.md`

## 검증 게이트 ★도구가 아니라 목적을 지킨다
- **게이트 4종은 도구가 없어도 건너뛰지 않는다** — 조사→`researcher`, 스펙화→`requirements-analyst`,
  수용 기준 대조→`spec-checker`, Phase 통합 테스트→`qa-tester`(foreground). 에이전트를 못 쓰면
  메인 agent가 **같은 원칙·같은 보고 형식으로 직접** 하고 그 사실을 커밋 메시지에 적는다
  (원칙은 각 `.claude/agents/*.md`에 있다).
- **사용자 호출 스킬**: `/code-review`(못 하면 커밋 메시지에 남기고 넘어간다) · `/security-review`
  (시점은 '보안 3층' ②·③, `origin` 원격 필요, 없으면 직접 점검) · `/debt-audit`(**Phase 3~5개마다 사용자에게 시점을 알린다** —
  목록만 만들고 정리 여부는 사용자 결정).
- **넓은 조사는 서브에이전트 병렬로**(코드 탐색 `Explore`·`general-purpose`, 데이터·API 조사
  `researcher`) — 메인은 **결론만** 보관한다. PLAN MODE는 plan.md 최초 작성·스코프 변경 시.

## 보안 3층 ★매 커밋이 아니라 "무엇을 바꿨나"로 걸린다 (2026-09-09 사용자 결정)
- **① 코딩 규칙 — 항상, 별도 단계 없음**: 질의·셸·파일 경로에 사용자 입력을 문자열로 조합하지 않는다
  (바인딩·ORM·경로 정규화) / 인증·권한은 fail-closed — 설정이 없으면 기동 거부 / 오류 응답에는 상태만,
  원인·경로·사용자명은 로그에만 / 비밀정보는 '불변 규칙'대로 `.env`. 확인은 사다리의 정적 분석에 묻어간다.
  **"입력이 DB에 닿는" 보통 기능은 여기서 끝난다** — 초기 개발의 거의 모든 기능이라 점검을 붙이면
  사다리가 무너진다. 목록 실물: `노하우_설계.md` 보안 기본값.
- **② 범위 점검 — 닿을 때만**: 인증·세션·권한 판정 / 파일 업로드·외부 실행물 / 결제 / 비밀정보 취급 /
  새 공개 표면(엔드포인트·웹훅·폼)을 넣거나 고치는 커밋은 **그 변경 범위만** 점검한다(`/security-review`,
  못 쓰면 `노하우_설계.md`의 공개 엔드포인트 점검 목록으로 직접) — 결과는 커밋 메시지에. 화면·문구·내부
  로직에는 붙이지 않는다. **bidwatch에서는 사용자 URL을 서버가 가져오는 AI 스크래퍼(F-009)가 상시 ② 대상이다.**
- **③ 전체 리뷰 — 공개 직전 1회**: 배포 Phase(011)에서 `/security-review` 전체(원격 없으면 직접).
  **통과가 공개의 선행 조건** — 조합돼야 드러나는 결함(권한 우회 경로·세션)은 ②로는 안 잡힌다.

## 구조
- **디렉토리 뼈대 (파일 생성 위치의 최종 기준)**:
  ```
  backend/app/          ← FastAPI (routers / schemas / services / models / tasks / utils)
  backend/alembic/      ← 마이그레이션 (versions/NNN_*.py)
  backend/tests/        ← 백엔드 테스트
  backend/scripts/      ← 운영·백필 스크립트
  frontend/src/         ← Next.js App Router (app / components / lib / stores / types)
  prototype/            ← 초기 HTML 프로토타입 (참고용, 신규 작업 대상 아님)
  tests/                ← 동봉 파이썬 도구(훅·측정기) 테스트
  docs/                 ← REQUIREMENTS.md, SCHEMA.md, system_design.md, api_spec.md, interface.md,
                           source_fields.md(출처별 표준 필드 원천·extra 키 의미), playbooks/,
                           requests/(bidwatch → bid-collectors 요청서 — 결과는 bid-collectors handover로 받는다)
  work_log/             ← plan.md, Phase_XXX.md
  scripts/              ← 동봉 측정·점검 스크립트
  ```
  **이 트리 밖에 새 최상위 디렉토리/파일을 만들지 말 것.** 필요하면 사용자 확인 후 이 목록부터 갱신.
- 설정/환경: `backend/.env`(로컬, gitignore) / 키 목록은 `.env.example`. 환경별 설정 파일은 값이 다르다 →
  **통째 복사 절대 금지**, 필요한 키만 한 줄씩.
- 새 기능 추가 위치 규칙: 백엔드는 도메인별 `routers/<도메인>.py` + 로직은 `services/` (라우터에 DB write 로직 금지),
  프론트는 페이지 `app/(app)/<경로>/page.tsx` + 서버 상태는 `lib/queries/use<도메인>.ts`.
- **같은 규칙이 두 곳 이상에 구현되면** 그 사실을 여기 적고, **두 구현을 대조하는 테스트를 반드시 만든다.**
  현재: 지역 17개 목록(백엔드 `services/region.py` 상수 ↔ 프론트는 API로 받음 — 이중 구현 아님).
  `docs/interface.md`는 bid-collectors 저장소와 **같은 문서가 두 곳**에 있다 — 변경은 bid-collectors가 하고
  `bid-collectors/docs/handover/v<버전>.md`로 넘긴다. bidwatch는 받은 파일로 통째 교체 후 `cmp`로 동일 확인, handover §5를 채운다(2026-09-25).

## 테스트 규칙
- 백엔드: `backend/.venv/Scripts/python.exe -m pytest backend/tests` (저장소 루트에서, 약 7초)
  ⚠️ **테스트가 로컬 개발 DB(`backend/.env`의 DATABASE_URL)를 그대로 쓴다** — `drop_all` 금지(시드 삭제 실사례).
- 프론트 정적 분석: `npx --prefix frontend tsc --noEmit -p frontend` · `npm --prefix frontend run lint`
- 동봉 파이썬 도구(훅·측정기)의 테스트: `python -m pytest tests` — **훅·측정기·CLAUDE.md를 고쳤을 때만**.
- **수용 기준 = 테스트 케이스**: REQUIREMENTS.md의 각 `[ ]` 항목이 테스트 하나에 대응해야 한다.
  **예외**: 표시 기준은 `(사용자 실테스트)` 항목 하나로 적고 테스트를 대응시키지 않는다('검증 강도'의 표시 변경).
- 데이터 관련 기준은 **실제 데이터에 직접 질의해** 검증한다(모킹으로 대신하지 않는다).
- 외부 API(data.go.kr·Claude API)에 의존하는 테스트는 **고정 응답으로 도는 것을 기본**으로 두고, 실호출은 분리한다.
  Redis가 없으므로 Celery 디스패치는 테스트에서 반드시 mock(`conftest.py`) — 안 하면 무한 대기.

## 작업 로그 원칙 (`work_log/Phase_XXX.md`) ★이 템플릿에서 가장 값이 큰 부분
- **Phase 로그는 실패한 접근·버린 대안이 있을 때만, 그때 바로, 2절로 쓴다.** 없으면 파일을 만들지 않고
  plan.md 체크박스 옆에 완료일만(2026-09-23 bidwatch 채택). 뒤늦게 커밋에서 복원하면 시행착오는 소실된다.
  ① 실패한 접근과 원인("X 시도 → 실패 → 원인 Y → Z로 해결", 엉뚱한 데서 찾은 오해 포함)
  ② 다음 세션이 알아야 할 것(결정 이유+버린 대안·외부 제약·운영 주의점 — 있는 것만).
  코드 구현 상세·git 히스토리·테스트 목록·검증 결과는 쓰지 않는다 — 코드는 What, 로그는 Why.
- Phase 001~008 로그(`Phase_001-003.md` ~ `Phase_008.md`)는 이식 전 형식이다 — 고치지 않고 이력으로 둔다.
- **로그에 적은 교훈은 저절로 다음 코드에 옮겨지지 않는다** — 가능하면 컴파일러·테스트·훅이
  잡는 구조로 강제하고, **문서는 구조로 강제할 수 없는 것에 쓴다.** 수동 등록부에는 대조 테스트를
  같은 커밋에. 실사례: `노하우_프로세스.md`

## 배포 체크리스트
**배포 방식은 Phase 011(배포)에서 정한다** — 그때 2~4를 채운다.
⏳만료: 아래 2~4를 채웠으면 이 두 줄을 지운다.

1. 검증 — **트랙별로 다르다**: 저위험은 커밋 때 사다리대로 한 것 위에 **아무것도 더 얹지 않는다**
   (로직이 바뀐 커밋이면 전체 1회가 이미 돌았다). 일반 트랙은 사다리의 일반 트랙 갈래 전부
   (전체 테스트·구버전 데이터 마이그레이션 실측·되돌리기 경로·사용자 실테스트).
2. `<빌드 명령 — Phase 011>`
3. `<배포 명령 — Phase 011>`
4. **실제 동작 확인** — `<확인 방법 — Phase 011>`
5. commit + push

## 알려진 함정 (스택 중립 + bidwatch에서 겪은 것. 새로 겪으면 `_TEMPLATE.md` 형식으로 플레이북 추가 + 여기 한 줄)

- **명령 앞에 무엇도 붙이지 말 것 — 한 호출 = 한 명령**(cd 접두사는 훅이 막는다). **허용된 앞머리로
  뒤엣것을 통과시키는 우회 금지 — 속도보다 우선.** 환경변수는 `settings.json`의 `env`에.
  상세: `노하우_승인_대기_최소화.md` §4
- **문서에 적힌 API·필드를 기억으로 쓰지 말 것**: 실제로 열어 이름·건수를 확인한다.
  200이 성공이 아닐 수 있다. 상세: `노하우_외부API_데이터.md` (실사례: 모달 extra 키가 문서와 달랐다 — `Phase_004.md`)
- **같은 원인의 버그를 고칠 때 다른 층에 같은 판정이 있는지 함께 본다**: 한쪽만 고쳐 재발한 실사례.
- **Windows + asyncpg는 `WindowsSelectorEventLoopPolicy` 필수** — Proactor 루프와 충돌한다(`Phase_001-003.md`).
- **라우트가 404인데 코드가 맞아 보이면 uvicorn reload 실패부터 의심** — 재시작으로 해결된 실사례(`Phase_004.md`).
- **data.go.kr 일일 호출 한도 1,000회**(수집+상세 합산) · 사전규격 API만 `ServiceKey` 대문자(`Phase_005.md`).
- **지역 정규화는 길이 내림차순 접두어 매칭** — `"경기도 광주시"`가 광주로 잡히는 중의성(`Phase_008.md`).
- **JSONB 함정 2가지** (2026-09-25): ① `extra->>'…'` 조건을 플래너가 1행으로 오판해 CTE+UPDATE가 중첩 루프로 20초 — 출처 단위 대량 판정은
  행을 읽어 파이썬에서(`refresh_revisions`) ② 모델에서 None을 넣은 JSONB는 SQL NULL이 아니라 JSON `null`로 저장된다 — SQL `IS NULL`로 못 찾는다.

## CLAUDE.md 비대화 방지 ★이 파일에는 상한이 있다

- **이 파일은 매 세션의 컨텍스트에 통째로 실린다** — 규칙이 많을수록 규칙별 준수율이 떨어진다.
  단위는 바이트가 아니라 **개수**다(IFScale 2025, Claude Code 세션 1,650건 요인 연구 2026).
- **상한: 규칙 100개**. 센다: `python scripts/count_claude_rules.py` —
  글머리·번호·표 행 하나 = 규칙 하나, `<...>` 플레이스홀더는 제외.
  Phase 종료 시 확인하고, 넘으면 이관한다:
  ① 구조(훅·테스트)로 강제된 항목은 한 줄+링크로 ② 정정의 역사·실험 전말은 플레이북으로
  (본문엔 현재 결론만) ③ 기능 상세는 REQUIREMENTS·Phase 로그로, 진행 서사는 plan.md로.
  남기는 것은 **"매 작업마다 적용되는 규칙 + 다음 세션이 첫 10분에 알아야 할 것"** 뿐이다.
- **중요한 규칙은 앞에 둔다.** ① 어기면 되돌리기 비싸고 구조(훅·테스트)로 강제되지 않는 것
  ② 매 작업마다 적용되는 것 ③ 참조용은 가운데 ④ 특정 시점에 찾아 읽는 것은 끝.
- **규칙은 빼기만 한다.** 한 번 통과하면 끝나는 게이트에는 모래시계(⏳) 만료 표시를 붙이고, 조건이
  채워지면 그 문장을 지운다. 만료 대상은 `count_claude_rules.py`가 Phase 종료 점검 때 함께 출력한다.
- **덧붙일 때 나갈 것을 함께 정한다.** **규칙·결론을 바꿨으면 같은 커밋에서 어긋난 문서를 grep으로
  대조한다 — 범위에 `docs/`만이 아니라 `.claude/skills/`·`.claude/agents/`를 넣는다.**
